#include <iostream>
#include <vector>

using namespace std;
int main(void)
{
	cin.tie(0);
	ios::sync_with_stdio(false);
	int n,m,k;
	cin >> n >> m >> k;

	vector<string> board(n);

	for(int i = 0; i < n; i++)
		cin >> board[i];



	return (0);
}
